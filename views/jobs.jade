extend layout

block content
	h1 Jobs available
	if jobs
		each job in jobs
			.panel.panel-primary
							.panel-heading Title: #{job.title} | Pay: £#{job.price} | Written by: #{job.client}
							.panel-body Summary: #{job.summary}