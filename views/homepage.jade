extend layout

block content
	h1 CAMUNITY
	.div 
		a.btn.btn-default(href='/login') Login as Client
	.div 
		a.btn.btn-default(href='/login') Login as Photographer

	hr

	.div 
		a.btn.btn-default(href='/profile') Profile
	.div 
		a.btn.btn-default(href='/account') Account
	.div 
		a.btn.btn-default(href='/jobs') Jobs
		
	hr

	h3 Photographers: Get paid!
	a.btn.btn-default(href='/authorize') Login on stripe

	hr
	h3 We welcome any donations
	form(action="/stripe" method="POST")
		script.stripe-button(src="https://checkout.stripe.com/checkout.js", data-key='#{key}', data-amount='2000', data-name='Pay me', data-description='£20 donation')